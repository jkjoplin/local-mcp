{
  "name": "local-mcp",
  "version": "1.0.0",
  "description": "MCP server that delegates tasks to local LLM servers running OpenAI-compatible APIs",
  "type": "module",
  "bin": {
    "local-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
